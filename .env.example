# AI API Keys (server-side only -- never prefixed with NEXT_PUBLIC_)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Vercel KV (optional -- graceful fallback to in-memory when not set)
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Contact form
CONTACT_EMAIL=nick@example.com

# Site URL (set to your production domain)
NEXT_PUBLIC_SITE_URL=https://nickwiley.dev
